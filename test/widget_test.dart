import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets(
    'Appointment Booking Application loads',
    (WidgetTester tester) async {
      // Basic project test.
      // The application can be tested through the running app.
      expect(true, isTrue);
    },
  );
}